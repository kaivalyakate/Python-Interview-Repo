def search(nums: list[int], target: int) -> int:
    start, end = 0, len(nums) - 1
    # Find the Minimum Index in the Sorted Rotated Array - O(log n)
    
    return -1


if __name__ == "__main__":
    print(search(nums=[1,3], target=3))